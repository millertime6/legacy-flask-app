# Give Gunicorn time to answer slow cold starts / DB work before nginx closes upstream.
# If you still see 499s, raise the load balancer idle timeout in the EB console (often 60s default).
proxy_connect_timeout 120s;
proxy_send_timeout 120s;
proxy_read_timeout 120s;
